<!-- app/views/public/tracking/not_found.html.erb -->
<div class="not-found-container">
  <div class="not-found-content">
    <div class="not-found-icon">
      <svg width="120" height="120" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
        <circle cx="12" cy="12" r="10"/>
        <path d="M16 16s-1.5-2-4-2-4 2-4 2"/>
        <line x1="9" y1="9" x2="9.01" y2="9"/>
        <line x1="15" y1="9" x2="15.01" y2="9"/>
      </svg>
    </div>
    
    <h1 class="not-found-title">Package Not Found</h1>
    <p class="not-found-description">
      We couldn't find a package with code <strong><%= params[:code] %></strong>
    </p>
    
    <div class="not-found-suggestions">
      <p class="suggestions-title">Please check:</p>
      <ul class="suggestions-list">
        <li>The tracking code is entered correctly</li>
        <li>There are no extra spaces in the code</li>
        <li>The package exists in our system</li>
      </ul>
    </div>
    
    <div class="not-found-actions">
      <button onclick="window.history.back()" class="btn-secondary">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M19 12H5M12 19l-7-7 7-7"/>
        </svg>
        Go Back
      </button>
      <a href="/" class="btn-primary">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/>
        </svg>
        Go Home
      </a>
    </div>
  </div>
</div>

<style>
  .not-found-container {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 2rem;
    background: linear-gradient(180deg, #1a1a2e 0%, #0f0f1e 100%);
  }
  
  .not-found-content {
    max-width: 500px;
    text-align: center;
    background: rgba(30, 30, 60, 0.8);
    border-radius: 1.5rem;
    padding: 3rem 2rem;
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
  }
  
  .not-found-icon {
    color: #a78bfa;
    margin-bottom: 2rem;
    display: flex;
    justify-content: center;
  }
  
  .not-found-title {
    font-size: 2rem;
    font-weight: 700;
    color: #f4f4f5;
    margin-bottom: 1rem;
  }
  
  .not-found-description {
    font-size: 1.125rem;
    color: #a1a1aa;
    margin-bottom: 2rem;
    line-height: 1.6;
  }
  
  .not-found-description strong {
    color: #a78bfa;
    font-weight: 600;
  }
  
  .not-found-suggestions {
    background: rgba(168, 85, 247, 0.1);
    border: 1px solid rgba(168, 85, 247, 0.2);
    border-radius: 0.75rem;
    padding: 1.5rem;
    margin-bottom: 2rem;
    text-align: left;
  }
  
  .suggestions-title {
    font-size: 0.875rem;
    font-weight: 600;
    color: #a78bfa;
    margin-bottom: 0.75rem;
  }
  
  .suggestions-list {
    list-style: none;
    padding: 0;
  }
  
  .suggestions-list li {
    font-size: 0.875rem;
    color: #e4e4e7;
    padding: 0.5rem 0;
    padding-left: 1.5rem;
    position: relative;
  }
  
  .suggestions-list li::before {
    content: "•";
    color: #a78bfa;
    font-weight: bold;
    position: absolute;
    left: 0.5rem;
  }
  
  .not-found-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }
  
  .btn-primary,
  .btn-secondary {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.875rem 1.5rem;
    border-radius: 0.75rem;
    font-size: 1rem;
    font-weight: 600;
    text-decoration: none;
    border: none;
    cursor: pointer;
    transition: all 0.2s;
  }
  
  .btn-primary {
    background: linear-gradient(135deg, #a78bfa 0%, #8b5cf6 100%);
    color: white;
  }
  
  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 16px rgba(168, 85, 247, 0.3);
  }
  
  .btn-secondary {
    background: rgba(168, 85, 247, 0.2);
    color: #a78bfa;
    border: 1px solid rgba(168, 85, 247, 0.3);
  }
  
  .btn-secondary:hover {
    background: rgba(168, 85, 247, 0.3);
    transform: translateY(-2px);
  }
  
  @media (max-width: 640px) {
    .not-found-content {
      padding: 2rem 1.5rem;
    }
    
    .not-found-title {
      font-size: 1.5rem;
    }
    
    .not-found-actions {
      flex-direction: column;
    }
    
    .btn-primary,
    .btn-secondary {
      width: 100%;
      justify-content: center;
    }
  }
</style>