<!-- app/views/public/tracking/index.html.erb -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Track Your Package - GLT Logistics</title>
  
  <!-- Favicon Links -->
  <link rel="icon" type="image/x-icon" href="/favicon.ico">
  <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
  <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
  <meta name="theme-color" content="#8b5cf6">
  
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      background: #0a0a0f;
      color: #e4e4e7;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      overflow-x: hidden;
    }

    /* Animated Background */
    .animated-bg {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      z-index: 0;
      overflow: hidden;
    }

    .gradient-orb {
      position: absolute;
      border-radius: 50%;
      filter: blur(80px);
      opacity: 0.25;
      animation: float 20s ease-in-out infinite;
    }

    .orb-1 {
      width: 500px;
      height: 500px;
      background: radial-gradient(circle, #8b5cf6 0%, transparent 70%);
      top: -10%;
      right: -10%;
      animation-delay: 0s;
    }

    .orb-2 {
      width: 400px;
      height: 400px;
      background: radial-gradient(circle, #a78bfa 0%, transparent 70%);
      bottom: -10%;
      left: -10%;
      animation-delay: 5s;
    }

    @keyframes float {
      0%, 100% { transform: translate(0, 0) scale(1); }
      33% { transform: translate(50px, -50px) scale(1.1); }
      66% { transform: translate(-30px, 30px) scale(0.9); }
    }

    /* Header */
    .header {
      position: relative;
      z-index: 2;
      padding: 1.5rem;
      background: rgba(10, 10, 15, 0.8);
      backdrop-filter: blur(20px);
      border-bottom: 1px solid rgba(139, 92, 246, 0.2);
    }

    .header-content {
      max-width: 1200px;
      margin: 0 auto;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .logo-container {
      display: flex;
      align-items: center;
      gap: 0.75rem;
    }

    .logo-image {
      height: 40px;
      width: auto;
    }

    .logo {
      font-size: 1.5rem;
      font-weight: 700;
      background: linear-gradient(135deg, #8b5cf6 0%, #a78bfa 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      text-decoration: none;
    }

    .back-link {
      color: #a78bfa;
      text-decoration: none;
      font-size: 0.9375rem;
      display: flex;
      align-items: center;
      gap: 0.5rem;
      transition: all 0.3s;
      padding: 0.5rem 1rem;
      border-radius: 8px;
    }

    .back-link:hover {
      color: #8b5cf6;
      background: rgba(139, 92, 246, 0.1);
    }

    /* Container */
    .container {
      position: relative;
      z-index: 1;
      flex: 1;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 3rem 1.5rem;
    }

    /* Tracking Card */
    .tracking-card {
      background: rgba(10, 10, 15, 0.8);
      backdrop-filter: blur(20px);
      border: 1px solid rgba(139, 92, 246, 0.3);
      border-radius: 32px;
      padding: 3.5rem;
      max-width: 600px;
      width: 100%;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
    }

    .tracking-icon {
      width: 100px;
      height: 100px;
      background: linear-gradient(135deg, rgba(139, 92, 246, 0.2) 0%, rgba(139, 92, 246, 0.1) 100%);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 2rem;
      font-size: 3rem;
      border: 2px solid rgba(139, 92, 246, 0.3);
    }

    .tracking-title {
      font-size: 2.25rem;
      font-weight: 800;
      background: linear-gradient(135deg, #ffffff 0%, #a78bfa 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      text-align: center;
      margin-bottom: 1rem;
      line-height: 1.2;
    }

    .tracking-subtitle {
      font-size: 1.0625rem;
      color: #a1a1aa;
      text-align: center;
      margin-bottom: 3rem;
      line-height: 1.6;
    }

    /* Form */
    .tracking-form {
      display: flex;
      flex-direction: column;
      gap: 1.5rem;
    }

    .form-group {
      display: flex;
      flex-direction: column;
      gap: 0.75rem;
    }

    .form-label {
      font-size: 0.9375rem;
      font-weight: 600;
      color: #a1a1aa;
    }

    .form-input {
      width: 100%;
      padding: 1.125rem;
      background: rgba(139, 92, 246, 0.05);
      border: 2px solid rgba(139, 92, 246, 0.3);
      border-radius: 16px;
      color: #f4f4f5;
      font-size: 1.0625rem;
      transition: all 0.3s;
      text-align: center;
      font-weight: 500;
      letter-spacing: 1px;
    }

    .form-input:focus {
      outline: none;
      border-color: #8b5cf6;
      background: rgba(139, 92, 246, 0.1);
      box-shadow: 0 0 0 4px rgba(139, 92, 246, 0.1);
    }

    .form-input::placeholder {
      color: #71717a;
      letter-spacing: normal;
    }

    .form-hint {
      font-size: 0.8125rem;
      color: #71717a;
      text-align: center;
    }

    .submit-button {
      width: 100%;
      padding: 1.125rem;
      background: linear-gradient(135deg, #8b5cf6 0%, #7c3aed 100%);
      border: none;
      border-radius: 16px;
      color: #fff;
      font-size: 1.0625rem;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.75rem;
      box-shadow: 0 4px 16px rgba(139, 92, 246, 0.4);
    }

    .submit-button:hover {
      background: linear-gradient(135deg, #7c3aed 0%, #6d28d9 100%);
      transform: translateY(-2px);
      box-shadow: 0 8px 24px rgba(139, 92, 246, 0.5);
    }

    .submit-button:active {
      transform: translateY(0);
    }

    .divider {
      display: flex;
      align-items: center;
      text-align: center;
      margin: 2rem 0 1.5rem;
    }

    .divider::before,
    .divider::after {
      content: '';
      flex: 1;
      border-bottom: 1px solid rgba(139, 92, 246, 0.2);
    }

    .divider-text {
      padding: 0 1rem;
      color: #71717a;
      font-size: 0.875rem;
      font-weight: 500;
    }

    .info-box {
      background: rgba(139, 92, 246, 0.05);
      border: 1px solid rgba(139, 92, 246, 0.2);
      border-radius: 16px;
      padding: 1.5rem;
    }

    .info-title {
      font-size: 0.875rem;
      font-weight: 600;
      color: #a78bfa;
      margin-bottom: 0.75rem;
    }

    .info-text {
      font-size: 0.875rem;
      color: #a1a1aa;
      line-height: 1.6;
    }

    .info-examples {
      margin-top: 0.75rem;
      padding-top: 0.75rem;
      border-top: 1px solid rgba(139, 92, 246, 0.1);
    }

    .info-example {
      font-size: 0.875rem;
      color: #71717a;
      margin-top: 0.5rem;
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .info-example code {
      background: rgba(139, 92, 246, 0.1);
      padding: 0.25rem 0.5rem;
      border-radius: 6px;
      color: #a78bfa;
      font-family: 'Courier New', monospace;
      font-size: 0.8125rem;
    }

    /* Responsive */
    @media (max-width: 640px) {
      .tracking-card {
        padding: 2rem 1.5rem;
      }

      .tracking-title {
        font-size: 1.75rem;
      }

      .tracking-subtitle {
        font-size: 0.9375rem;
      }

      .back-link span {
        display: none;
      }
    }
  </style>
</head>
<body>
  <!-- Animated Background -->
  <div class="animated-bg">
    <div class="gradient-orb orb-1"></div>
    <div class="gradient-orb orb-2"></div>
  </div>

  <!-- Header -->
  <header class="header">
    <div class="header-content">
      <div class="logo-container">
        <img src="/images/logo.png" alt="GLT Logistics" class="logo-image">
        <a href="/" class="logo">GLT Logistics</a>
      </div>
      <a href="/" class="back-link">
        <span>←</span>
        <span>Back to Home</span>
      </a>
    </div>
  </header>

  <!-- Container -->
  <div class="container">
    <div class="tracking-card">
      <div class="tracking-icon">📦</div>
      
      <h1 class="tracking-title">Track Your Package</h1>
      <p class="tracking-subtitle">
        Enter your tracking code to view real-time updates and delivery status
      </p>

      <form class="tracking-form" action="/public/track" method="get" onsubmit="return handleSubmit(event)">
        <div class="form-group">
          <label class="form-label" for="tracking-code">Tracking Code</label>
          <input 
            type="text" 
            id="tracking-code" 
            name="code"
            class="form-input" 
            placeholder="NRB-008-MCH or NRB-005"
            required
            pattern="[A-Z]{3}-\d{3}(-[A-Z]{3})?"
            title="Format: XXX-### or XXX-###-XXX (e.g., NRB-005 or NRB-008-MCH)"
          />
          <p class="form-hint">Format: XXX-### or XXX-###-XXX</p>
        </div>

        <button type="submit" class="submit-button">
          <span>Track Package</span>
          <span>→</span>
        </button>
      </form>

      <div class="divider">
        <span class="divider-text">Need help?</span>
      </div>

      <div class="info-box">
        <p class="info-title">Where to find your tracking code</p>
        <p class="info-text">
          Your tracking code can be found in the confirmation email or SMS you received when your package was created.
        </p>
        <div class="info-examples">
          <p class="info-example">✓ Example: <code>NRB-008-MCH</code></p>
          <p class="info-example">✓ Example: <code>NRB-005</code></p>
        </div>
      </div>
    </div>
  </div>

  <script>
    function handleSubmit(event) {
      event.preventDefault();
      const input = document.getElementById('tracking-code');
      const code = input.value.trim().toUpperCase();
      
      // Validate format: XXX-### or XXX-###-XXX
      const pattern = /^[A-Z]{3}-\d{3}(-[A-Z]{3})?$/;
      
      if (!pattern.test(code)) {
        alert('Please enter a valid tracking code format:\nXXX-### (e.g., NRB-005)\nor\nXXX-###-XXX (e.g., NRB-008-MCH)');
        return false;
      }
      
      if (code) {
        window.location.href = `/public/track/${code}`;
      }
      
      return false;
    }

    // Auto-uppercase and format
    document.getElementById('tracking-code').addEventListener('input', function(e) {
      e.target.value = e.target.value.toUpperCase();
    });
  </script>
</body>
</html>