<!-- app/views/admin/conversations/index.html.erb -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Support Conversations - GLT Logistics Admin</title>
  
  <!-- Favicon Links -->
  <link rel="icon" type="image/x-icon" href="/favicon.ico">
  <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
  <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
  <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
  
  <!-- Theme Color -->
  <meta name="theme-color" content="#8b5cf6">
  <meta name="msapplication-TileColor" content="#8b5cf6">
  
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      background: #0a0a0f;
      color: #e4e4e7;
      min-height: 100vh;
    }
    
    .container { max-width: 1400px; margin: 0 auto; padding: 20px; }
    
    .header {
      background: linear-gradient(135deg, #8b5cf6 0%, #7c3aed 100%);
      color: white;
      padding: 30px;
      border-radius: 20px;
      margin-bottom: 30px;
      box-shadow: 0 8px 32px rgba(139, 92, 246, 0.3);
    }
    
    .header h1 { font-size: 2rem; margin-bottom: 10px; font-weight: 700; }
    .header p { opacity: 0.9; margin-bottom: 20px; }
    
    .actions {
      display: flex;
      gap: 10px;
      flex-wrap: wrap;
    }
    
    .btn {
      padding: 10px 20px;
      border: none;
      border-radius: 12px;
      cursor: pointer;
      font-weight: 600;
      transition: all 0.3s;
      text-decoration: none;
      display: inline-block;
    }
    
    .btn-primary {
      background: rgba(255, 255, 255, 0.2);
      color: white;
      border: 2px solid rgba(255, 255, 255, 0.3);
    }
    
    .btn-primary:hover {
      background: rgba(255, 255, 255, 0.3);
      transform: translateY(-2px);
    }
    
    .btn-success {
      background: linear-gradient(135deg, #10b981 0%, #059669 100%);
      color: white;
    }
    
    .btn-success:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 24px rgba(16, 185, 129, 0.3);
    }
    
    .stats {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 20px;
      margin-bottom: 30px;
    }
    
    .stat-card {
      background: rgba(139, 92, 246, 0.1);
      border: 1px solid rgba(139, 92, 246, 0.3);
      border-radius: 20px;
      padding: 25px;
      transition: all 0.3s;
    }
    
    .stat-card:hover {
      background: rgba(139, 92, 246, 0.15);
      border-color: rgba(139, 92, 246, 0.5);
      transform: translateY(-4px);
    }
    
    .stat-card h3 {
      font-size: 0.9rem;
      color: #a1a1aa;
      margin-bottom: 10px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }
    
    .stat-card .number {
      font-size: 2.5rem;
      font-weight: 800;
      background: linear-gradient(135deg, #8b5cf6 0%, #a78bfa 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }
    
    .filters {
      background: rgba(139, 92, 246, 0.1);
      border: 1px solid rgba(139, 92, 246, 0.3);
      padding: 20px;
      border-radius: 20px;
      margin-bottom: 20px;
    }
    
    .filters form {
      display: flex;
      gap: 10px;
      align-items: center;
      flex-wrap: wrap;
    }
    
    .filters select {
      padding: 10px 15px;
      border: 2px solid rgba(139, 92, 246, 0.3);
      border-radius: 10px;
      background: rgba(10, 10, 15, 0.8);
      color: #e4e4e7;
      font-size: 14px;
      cursor: pointer;
    }
    
    .filters select:focus {
      outline: none;
      border-color: #8b5cf6;
      box-shadow: 0 0 0 3px rgba(139, 92, 246, 0.1);
    }
    
    .conversations-list {
      background: rgba(10, 10, 15, 0.8);
      border: 1px solid rgba(139, 92, 246, 0.2);
      border-radius: 20px;
      overflow: hidden;
      backdrop-filter: blur(20px);
    }
    
    .conversation-item {
      padding: 20px;
      border-bottom: 1px solid rgba(139, 92, 246, 0.1);
      display: flex;
      justify-content: space-between;
      align-items: start;
      transition: all 0.3s;
    }
    
    .conversation-item:hover {
      background: rgba(139, 92, 246, 0.1);
    }
    
    .conversation-item:last-child { border-bottom: none; }
    
    .conversation-content { flex: 1; }
    
    .conversation-title {
      font-weight: 700;
      margin-bottom: 8px;
      font-size: 1.1rem;
      color: #f4f4f5;
    }
    
    .conversation-meta {
      font-size: 0.85rem;
      color: #a1a1aa;
      margin-top: 8px;
    }
    
    .conversation-actions {
      display: flex;
      gap: 10px;
    }
    
    .badge {
      padding: 4px 12px;
      border-radius: 12px;
      font-size: 0.75rem;
      font-weight: 600;
      margin-left: 10px;
      text-transform: uppercase;
    }
    
    .badge-pending {
      background: rgba(245, 158, 11, 0.2);
      color: #fbbf24;
      border: 1px solid rgba(245, 158, 11, 0.3);
    }
    
    .badge-in_progress {
      background: rgba(59, 130, 246, 0.2);
      color: #60a5fa;
      border: 1px solid rgba(59, 130, 246, 0.3);
    }
    
    .badge-resolved {
      background: rgba(16, 185, 129, 0.2);
      color: #34d399;
      border: 1px solid rgba(16, 185, 129, 0.3);
    }
    
    .empty-state {
      text-align: center;
      padding: 60px 20px;
      color: #a1a1aa;
    }
    
    .empty-state-icon { font-size: 3rem; opacity: 0.3; margin-bottom: 15px; }
    
    @media (max-width: 768px) {
      .stats { grid-template-columns: repeat(2, 1fr); }
      .conversation-item { flex-direction: column; gap: 15px; }
      .conversation-actions { width: 100%; }
      .btn { width: 100%; text-align: center; }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>💬 Support Conversations</h1>
      <p>Manage customer support tickets and conversations</p>
      <div class="actions">
        <a href="<%= test_admin_conversations_path %>" class="btn btn-success">🧪 Test Messaging</a>
        <a href="<%= admin_root_path %>" class="btn btn-primary">← Back to Admin</a>
      </div>
    </div>

    <div class="stats">
      <div class="stat-card">
        <h3>Total Conversations</h3>
        <div class="number"><%= @stats[:total] %></div>
      </div>
      <div class="stat-card">
        <h3>Pending</h3>
        <div class="number"><%= @stats[:pending] %></div>
      </div>
      <div class="stat-card">
        <h3>In Progress</h3>
        <div class="number"><%= @stats[:in_progress] %></div>
      </div>
      <div class="stat-card">
        <h3>Resolved</h3>
        <div class="number"><%= @stats[:resolved] %></div>
      </div>
    </div>

    <div class="filters">
      <%= form_with url: admin_conversations_path, method: :get, local: true do |f| %>
        <%= f.select :status, [
          ['All Status', ''],
          ['Pending', 'pending'],
          ['In Progress', 'in_progress'],
          ['Resolved', 'resolved']
        ], {}, { onchange: 'this.form.submit()' } %>
        
        <%= f.select :priority, [
          ['All Priority', ''],
          ['High', 'high'],
          ['Normal', 'normal'],
          ['Low', 'low']
        ], {}, { onchange: 'this.form.submit()' } %>
      <% end %>
    </div>

    <div class="conversations-list">
      <% if @conversations.any? %>
        <% @conversations.each do |conversation| %>
          <div class="conversation-item">
            <div class="conversation-content">
              <div class="conversation-title">
                <%= conversation.title || "Conversation ##{conversation.id}" %>
                <% if conversation.status %>
                  <span class="badge badge-<%= conversation.status %>">
                    <%= conversation.status.humanize %>
                  </span>
                <% end %>
              </div>
              
              <% if conversation.customer %>
                <div style="color: #e4e4e7; margin-bottom: 5px;">Customer: <%= conversation.customer.email %></div>
              <% end %>
              
              <% if conversation.assigned_agent %>
                <div style="color: #e4e4e7; margin-bottom: 5px;">Agent: <%= conversation.assigned_agent.email %></div>
              <% end %>
              
              <div class="conversation-meta">
                Ticket ID: <%= conversation.ticket_id || conversation.current_ticket_id || 'N/A' %> | 
                Messages: <%= conversation.messages.count %> | 
                <%= time_ago_in_words(conversation.created_at) %> ago
              </div>
            </div>
            
            <div class="conversation-actions">
              <a href="<%= admin_conversation_path(conversation) %>" class="btn btn-primary">View</a>
            </div>
          </div>
        <% end %>
      <% else %>
        <div class="empty-state">
          <div class="empty-state-icon">💬</div>
          <p>No conversations found</p>
        </div>
      <% end %>
    </div>
  </div>
</body>
</html>